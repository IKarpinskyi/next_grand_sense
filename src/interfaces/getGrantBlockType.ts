import { Button } from "./button"
import { ImageWrapper } from "./imageData"

export type GetGrantBlockType = {
    heading: string
    headingDescription: string
    title: string
    description: string
    image: ImageWrapper
    contactButton: Button
    background: ImageWrapper
}